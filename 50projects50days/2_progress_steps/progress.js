let activeCircle = 1

document.addEventListener('DOMContentLoaded', () => {
    const circles = document.getElementByClass('circle');

    const next = document.getElementById('next')
    const prev = document.getElementById('prev')

    

})